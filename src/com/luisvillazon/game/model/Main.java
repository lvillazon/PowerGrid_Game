package com.luisvillazon.game.model;

/*
Customer class
Name
Power required (day, hour) - function returning the power requirements for that time
Power received
Minimum voltage - if these are exceeded you lose the level
Maximum voltage

Grid class
Power stations - list of stations conencted to the grid
Customers - list of customers connected to the grid
Current supply
Current draw
Voltage
 */

public class Main {

    public static void main(String[] args) {
        // create a dummy station to test
/*        CoalFired test = new CoalFired();
        test.setThrottle(50);
        int oldThrot = test.throttle.getCurrentThrottle();
        while (test.throttle.getCurrentThrottle() != 50) {
            if (oldThrot != test.throttle.getCurrentThrottle()) {
                System.out.println(test.throttle.getCurrentThrottle());
                oldThrot = test.throttle.getCurrentThrottle();
            }
        }
*/
    }
}

